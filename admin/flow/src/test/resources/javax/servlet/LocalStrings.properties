# Stub resource bundle so GenericServlet's static initializer succeeds in
# container-less smoke tests (the OCCAS javaee-api jar is API stubs only and
# does not bundle javax/servlet/LocalStrings). Empty on purpose.
