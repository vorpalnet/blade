# Test-only: javax.servlet.GenericServlet loads this bundle in a static initializer.
# The servlet API on the compile classpath omits it, so without this file no servlet
# can be constructed in a unit test.
