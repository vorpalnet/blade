# Minimal build profile - core routing only
#
# Add modules to include in the build and EAR.
# Use the full project directory name.

# Libraries (required by every WAR)
framework
shared

# Services
proxy-registrar
proxy-router
