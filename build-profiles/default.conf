# Default build profile - the modules built when no profile is specified.
#
# Add modules to include in the build. Use the full project directory name.
# Anything NOT listed here is excluded via -Dskip.<name>.

# Libraries (framework + shared are required by every WAR; fsmar3 is opt-in)
framework
shared
fsmar

# Admin apps
configurator
crud-editor
watcher

# Services
acl
analytics
hold
options
presence
proxy-balancer
proxy-block
proxy-registrar
proxy-router
queue
tpcc
transfer

# Test applications
test-b2bua
test-uac
test-uas
