# Production build profile - admin apps + services + test apps
#
# Add modules to include in the build. Use the full project directory name.
# Anything NOT listed here is excluded via -Dskip.<name>.

# Libraries
framework
shared
fsmar

# Admin apps + the admin EAR (apps/admin -> blade-admin.ear). Drop an app here
# and it drops out of the EAR (each rides its own ear-<name> profile).
portal
api
redirect
configurator
files
crud-editor
flow
tuning
logs
analytics-console
admin

# Production services + the cluster EAR (apps/services -> blade-services.ear).
# acl moved to proto/ (not shipped). proxy-registrar IS built but ships ONLY in
# blade-test.ear (it's wide open / no auth) — never in blade-services.ear.
analytics
context
hold
options
presence
proxy-balancer
proxy-block
proxy-registrar
irouter
queue
tpcc
transfer
crud
services

# Test apps + the test EAR (apps/test -> blade-test.ear -> engine0). blade-test.ear
# bundles every service (incl. proxy-registrar) plus these harness apps.
test-b2bua
test-uac
test-uas
test
